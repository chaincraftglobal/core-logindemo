services:
  - type: web
    name: core-logindemo
    env: node
    buildCommand: pnpm install && pnpm run build
    startCommand: pnpm start
    plan: free
    envVars:
      - key: PORT
        value: 8080
